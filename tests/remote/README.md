# Remote deployment tests

This directory is intentionally empty of executable tests until the first `.remote/` snapshot is pulled from Apps Script.

Once `.remote/` exists, add tests that compare the deployed snapshot with the intended runtime contract. Keep static source checks and mocked behavior checks separate from tests that require execution in Apps Script.
